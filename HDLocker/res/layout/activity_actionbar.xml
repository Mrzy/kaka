<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/actionbar_rootview"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".NoBoringActionBarActivity" >

    <ListView
        android:id="@+id/listview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/white"
        android:divider="@null"
        android:scrollbars="none" />

    <FrameLayout
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="@dimen/fast_actionbar_header_height"
        android:layout_gravity="top" >

        <cn.zmdx.kaka.fast.locker.widget.KenBurnsView
            android:id="@+id/header_picture"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/picture0" />

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent" >

            <ImageView
                android:id="@+id/header_logo"
                android:layout_width="@dimen/fast_actionbar_header_logo"
                android:layout_height="@dimen/fast_actionbar_header_logo"
                android:layout_centerHorizontal="true"
                android:layout_marginTop="30dp"
                android:src="@drawable/fast_locker_logo" />

            <TextView
                android:id="@+id/fastlocker_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_below="@+id/header_logo"
                android:layout_centerHorizontal="true"
                android:layout_marginTop="20dp"
                android:text="@string/fast_locker_name_zh"
                android:textColor="#ffffff"
                android:textSize="22sp" />
        </RelativeLayout>
    </FrameLayout>

    <ImageView
        android:id="@+id/fast_comment"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end|bottom"
        android:layout_marginBottom="15dp"
        android:layout_marginEnd="10dp"
        android:layout_marginRight="10dp"
        android:background="@drawable/round_button"
        android:src="@drawable/fast_comment" />

</FrameLayout>